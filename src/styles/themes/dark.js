import commonStyles from "./common";

const dark = {
    ...commonStyles,
    textColor: '#fff',
    background: '#444',
    backgroundDarker: '#222',
    buttonBorder: 'black'
}

export default dark;