import commonStyles from "./common";

const dark = {
    ...commonStyles,
    textColor: '#fff',
    background: '#444',
    backgroundDarker: '#222',
    buttonBorder: '#111',
    clickColor: '#000',
    searchIconBackground: '#666',
    inputFocus: 'black'
}

export default dark;