import commonStyles from "./common";

const dark = {
    ...commonStyles,
    textColor: '#fff',
    background: '#444',
    backgroundDarker: '#222',
    buttonBorder: '#111',
    clickColor: '#000'
}

export default dark;